import React from "react";
import { View } from "react-native";

const Separator = () => {
  return (
    <View
      style={{
        height: 30,
        borderWidth: 1,
        borderColor: "#f0f0f0",
        marginHorizontal: 5,
      }}
    />
  );
};

export default Separator;
